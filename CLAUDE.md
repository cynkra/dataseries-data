# CLAUDE.md

See **[AGENTS.md](AGENTS.md)** for how to work in this repo — the source-of-truth
layering (datasheets generate everything in `data/`/`catalog.json`), how to
regenerate after an edit, and which docs to read first.

Read it **before** changing any dataset, data file, datasheet, or the catalog.
